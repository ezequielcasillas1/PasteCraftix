# Environment and Config
.env
config.js

# Visual Studio
.vs/
*.vsidx
*.wsuo
*.db-shm
*.db-wal

# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~

# Build outputs
dist/
build/
*.zip

# Logs
logs/
*.log

# Temporary files
tmp/
temp/
*.tmp

# Browser test profiles
tests/.browser-profiles/

# Chrome Extension
*.crx
*.pem

# Bundled libraries (too large for git — download locally)
extension/lib/*
!extension/lib/lucide.min.js

# Sensitive data — never commit
secrets/
credentials/
*.key
*.cert
*.pem
*.p12
*.pfx
*.secret
*.secrets
.env
.env.*
!.env.example
service_role*
*_service_role*
*SERVICE_ROLE*
supabase_keys.*

# Local-only / generated in this workspace
supabase/.temp/

# Local configs/docs (ignore)
config.toml
PRODUCTION_*.md


# Local Netlify folder
.netlify

# Admin dashboard — localhost only, never committed
admin/

# Cursor AI editor config — local dev tool, not repo material
.cursor/*
!.cursor/rules/
!.cursor/rules/**
